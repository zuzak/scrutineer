extends ../layout
block title
  title= h1 ? h1: "About you"
block content
  - data = data ? data : user
  h1= h1 ? h1 : "About you"
  p We hold the following information about you:
  table
    tr
      th User name
      td= data.username
    tr
      th Password
      td: emph [Not shown]
    tr
        th Email address
        td= data.email

  p If any of this is wrong, you can&nbsp;
    a(href='/amend-details') amend your details
    span .

  a(href='/') Return to home page